language: node_js
node_js:
  - '8'
matrix:
  include:
  - os: linux
    dist: xenial
    sudo: required
  - os: osx
cache:
  yarn: true
notifications:
  email: false
script:
  - npm install && bin/bash -c "yarn --link-duplicates --pure-lockfile && yarn release --linux"
